'use strict';

function ConnectionSocket(host, port, sufix) {
  var connection = this;

  connection.post = function(address, params) {

  };

  connection.get = function(address, params) {

  };

  connection.put = function(address, params) {

  };

  connection.delete = function(address, params) {

  };

  connection.print = function(address, params) {

  };
}

module.exports = ConnectionSocket;
